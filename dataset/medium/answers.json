[
  {
    "snippet_id": "m1",
    "language": "python",
    "context": "Sends an outbound API request with retry support for transient failures",
    "pr_description": "Added retry helper for the billing gateway client",
    "failed_test": "assert fetch_with_retries(client, request, max_attempts=3) calls send 3 times  # Only 2 attempts happen",
    "bug_type": "off_by_one",
    "explanation": "The retry loop stops one attempt too early because the upper bound excludes max_attempts.",
    "severity": "high",
    "suggested_fix": "for attempt in range(1, max_attempts + 1):",
    "grading_hints": {
      "explanation_terms": ["upper bound", "max_attempts", "one attempt too early"],
      "fix_terms": ["range", "max_attempts + 1"],
      "expected_term_limit": 5
    }
  },
  {
    "snippet_id": "m2",
    "language": "python",
    "context": "Stores cache metadata tags for invalidation in the content API",
    "pr_description": "Added helper for tagging cached CMS responses",
    "failed_test": "assert add_cache_tag('post:1', 'news') != add_cache_tag('post:2', 'sports')  # Tags leak across requests",
    "bug_type": "mutable_default_arg",
    "explanation": "The default tags list is shared across calls, so cache tags leak between requests.",
    "severity": "high",
    "suggested_fix": "def add_cache_tag(key, tag, tags=None):\n    if tags is None:\n        tags = []",
    "grading_hints": {
      "explanation_terms": ["shared", "default tags list", "leak"],
      "fix_terms": ["tags=None", "tags is None", "tags = []"],
      "expected_term_limit": 5
    }
  },
  {
    "snippet_id": "m3",
    "language": "python",
    "context": "Extracts the event metadata from a payment webhook payload",
    "pr_description": "Added webhook payload parser for the payments service",
    "failed_test": "assert parse_webhook_event(None) is None  # Crashes when the provider sends an empty payload",
    "bug_type": "missing_edge_case",
    "explanation": "The parser assumes payload is always present and well-formed, so None crashes immediately.",
    "severity": "medium",
    "suggested_fix": "if not payload:\n    return None\nevent_type = payload[\"type\"]",
    "grading_hints": {
      "explanation_terms": ["payload", "none", "empty payload", "crashes"],
      "fix_terms": ["if not payload", "return None"],
      "expected_term_limit": 5
    }
  },
  {
    "snippet_id": "m4",
    "language": "python",
    "context": "Appends an item to a list and returns it, used in a cart accumulation helper",
    "pr_description": "Added append_item utility for the shopping cart module",
    "failed_test": "assert append_item('a') != append_item('b')  # Both calls share the same list",
    "bug_type": "mutable_default_arg",
    "explanation": "Using a mutable list as a default argument means all calls share the same list object across invocations.",
    "severity": "medium",
    "suggested_fix": "def append_item(item, lst=None):\n    if lst is None:\n        lst = []\n    lst.append(item)\n    return lst",
    "grading_hints": {
      "explanation_terms": ["mutable", "default", "shared", "list", "invocation"],
      "fix_terms": ["None", "if lst is None", "lst = []"],
      "expected_term_limit": 5
    }
  },
  {
    "snippet_id": "m5",
    "language": "python",
    "context": "Checks whether a given year is a leap year in a date utility module",
    "pr_description": "Added is_leap_year helper for the calendar utilities package",
    "failed_test": "assert is_leap_year(2000) is True  # Returns False — misses the 400-year rule",
    "bug_type": "wrong_logic",
    "explanation": "The condition correctly handles the 4-year and 100-year rules but omits the 400-year exception, causing years like 2000 to be incorrectly classified.",
    "severity": "high",
    "suggested_fix": "return year % 4 == 0 and (year % 100 != 0 or year % 400 == 0)",
    "grading_hints": {
      "explanation_terms": ["400", "century", "exception", "divisible", "rule"],
      "fix_terms": ["year % 400", "or year % 400 == 0"],
      "expected_term_limit": 5
    }
  },
  {
    "snippet_id": "m6",
    "language": "python",
    "context": "Divides two numbers safely in a financial calculation helper",
    "pr_description": "Added safe_divide utility for the payments processing module",
    "failed_test": "assert safe_divide(10, 0) is None  # Raises ZeroDivisionError instead",
    "bug_type": "missing_edge_case",
    "explanation": "The function performs division without checking whether the denominator is zero, raising an unhandled ZeroDivisionError at runtime.",
    "severity": "medium",
    "suggested_fix": "if b == 0:\n    return None\nreturn a / b",
    "grading_hints": {
      "explanation_terms": ["zero", "denominator", "ZeroDivisionError", "edge case", "check"],
      "fix_terms": ["b == 0", "return None", "if b"],
      "expected_term_limit": 5
    }
  }
]
